import React from 'react'
import { Searchbar } from 'react-native-paper'
const TopSearchBar = () => {
    return <Searchbar placeholder="Search place" value={searchQuery} />
}

export default TopSearchBar
